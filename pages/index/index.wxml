<!--index.wxml-->
<scroll-view class="scrollarea" scroll-y type="list">
  <view class="container">
    <view class="userinfo">
      <block>
      <text>所在位置：{{location.address}}</text>
        <button class="avatar-wrapper" type="primary" bind:tap="getUserLocation">
          <!-- <image class="avatar" src="{{userInfo.avatarUrl}}"></image> -->
          获取当前位置
        </button>
      </block>
    </view>
  </view>
</scroll-view>
