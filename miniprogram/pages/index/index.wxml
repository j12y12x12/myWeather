<!--index.wxml-->
<navigation-bar title="乾坤天气" back="{{false}}" color="black" background="#FFF"></navigation-bar>
<scroll-view class="scrollarea" scroll-y type="list">
  <view class="container">
    <view class="userinfo">
      <block>
      <text>所在城市：未知</text>
        <button class="avatar-wrapper" type="primary" bind:tap="getUserLocation">
          <!-- <image class="avatar" src="{{userInfo.avatarUrl}}"></image> -->
          获取当前位置
        </button>
      </block>
    </view>
  </view>
</scroll-view>
